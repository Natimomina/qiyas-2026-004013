# Models

This folder is used to store trained machine learning models.

The CNN model trained in the Jupyter Notebook can be saved here after training.

Example:

```text
models/
└── amharic_geez_digit_cnn.keras
```

The model can later be loaded and used to recognize the nine Ge'ez digits:

**፩ ፪ ፫ ፬ ፭ ፮ ፯ ፰ ፱**
