# Processed Data

This folder contains the preprocessed images generated by the Jupyter Notebook.

The preprocessing includes:

* Converting images to grayscale
* Finding the maximum image width and height
* Padding smaller images with zeros
* Standardizing all images to the same dimensions

The processed dataset is generated automatically from the raw dataset and does not need to be manually created.
